//JQuery is basically 
$('div').text("Something else")//changes all elements
$("div.item").text("Another")
$("#one").text('first item')

let i = document.getElementById('one')
$('one')[0] == document.getElementById('one')//true
//here we have written $('one')[0] instead of $('one')
//because $() returns an array so we use it like an array
$('#one')//array

$('one').text('first')
$('div')[0]
$('div')[1]
